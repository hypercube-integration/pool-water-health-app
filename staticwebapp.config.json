{
  "routes": [
    { "route": "/api/submitReading", "allowedRoles": ["authenticated"] },
    { "route": "/api/updateReading", "allowedRoles": ["authenticated"] },
    { "route": "/api/deleteReading", "allowedRoles": ["authenticated"] },
    { "route": "/api/exportCSV",   "allowedRoles": ["authenticated"] }

    /* If you also want to protect reads, uncomment this:
    ,{ "route": "/api/getReadings", "allowedRoles": ["authenticated"] }
    */
  ]
}
